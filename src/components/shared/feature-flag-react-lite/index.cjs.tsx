import useFeatureFlags from './use-feature-flag'
import FeatureFlagProvider from './feature-flag-provider'

Object.assign(FeatureFlagProvider, {
  useFeatureFlags
})

export default FeatureFlagProvider

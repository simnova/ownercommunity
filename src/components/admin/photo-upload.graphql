mutation PhotoUploadCreateAuthHeaderForProfilePhoto($input: ProfilePhotoImageInput!) {
  createAuthHeaderForProfilePhoto(input: $input) {
    authHeader
    blobName
    requestDate
    success
    errorMessage
  }
}
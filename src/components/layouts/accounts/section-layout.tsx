import { MasterLayout } from '../../ui/organisms/master-layout/master-layout';

export const SectionLayout: React.FC<any> = () => {
  return <MasterLayout hasHeaderDropdownMenu={false} hasSidebar={false} pageLayouts={[]} />;
};

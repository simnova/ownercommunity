"""
A payment object.
"""
type Payment implements MongoBase {
  paymentKeyId: String

  id: ObjectID!
  schemaVersion: String
  createdAt: DateTime
  updatedAt: DateTime

  tags: [String]
}

extend type Query {
  paymentKeyId: String
}